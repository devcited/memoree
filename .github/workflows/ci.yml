name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_VERSION: "1.94.0"
  ASTRO_TELEMETRY_DISABLED: "1"

jobs:
  rust:
    name: Rust (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-14]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Install Rust toolchain
        run: rustup toolchain install "$RUST_VERSION" --profile minimal --component rustfmt,clippy --no-self-update
      - name: Select Rust toolchain
        run: rustup override set "$RUST_VERSION"
      - uses: Swatinem/rust-cache@v2
      - name: Formatting
        if: runner.os == 'Linux'
        run: cargo fmt --all -- --check
      - name: Clippy
        if: runner.os == 'Linux'
        run: cargo clippy --locked --all-targets -- -D warnings
      - name: Test
        run: cargo test --locked
      - name: Verify real v0.2.0 upgrade path
        if: runner.os == 'Linux'
        shell: bash
        run: |
          fixture_dir="$(mktemp -d)"
          archive="$fixture_dir/memoree-x86_64-unknown-linux-musl.tar.gz"
          curl --proto '=https' --tlsv1.2 -fsSL --retry 3 \
            -o "$archive" \
            https://memoree.dev/releases/download/v0.2.0/memoree-x86_64-unknown-linux-musl.tar.gz
          curl --proto '=https' --tlsv1.2 -fsSL --retry 3 \
            -o "$archive.sha256" \
            https://memoree.dev/releases/download/v0.2.0/memoree-x86_64-unknown-linux-musl.tar.gz.sha256
          expected="$(awk 'NR == 1 { print $1 }' "$archive.sha256")"
          actual="$(sha256sum "$archive" | awk '{ print $1 }')"
          test "$actual" = "$expected"
          tar -xzf "$archive" -C "$fixture_dir"
          MEMOREE_V02_BINARY="$fixture_dir/memoree-x86_64-unknown-linux-musl/memoree" \
            cargo test --locked --test upgrade_from_v02 -- --ignored --nocapture
      - name: Verify stable installer reconciliation
        if: runner.os == 'Linux'
        run: sh scripts/test-installer-upgrade.sh
      - name: Retrieval regression suite
        run: cargo run --locked --bin memoree-eval -- eval/corpus/v2
      - name: Verify generated web contracts
        if: runner.os == 'Linux'
        run: |
          sh scripts/export-web-contracts.sh
          git diff --exit-code -- site/public/schema/v1.json site/public/agents/instructions.md
  dependencies:
    name: Dependency policy
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          log-level: warn
          command: check

  secrets:
    name: Secret history
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITLEAKS_VERSION: "8.30.1"

  site:
    name: Site
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: site
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.1
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: site/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - name: Validate installer syntax
        run: sh -n public/install.sh

  release-plan:
    name: Release plan
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - name: Verify synchronized release metadata
        run: sh scripts/check-release-metadata.sh
      - name: Install cargo-dist
        shell: bash
        run: curl --proto '=https' --tlsv1.2 -LsSf https://github.com/axodotdev/cargo-dist/releases/download/v0.32.0/cargo-dist-installer.sh | sh
      - run: dist generate --check
      - run: dist plan --output-format=json > /dev/null
